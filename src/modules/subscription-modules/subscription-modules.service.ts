import { Injectable } from '@nestjs/common';

@Injectable()
export class SubscriptionModulesService {
  // constructor(private readonly prisma: PrismaService) {}
  // async create(createSubscriptionModuleInput: CreateSubscriptionModuleInput) {
  //   return await this.prisma.subscriptionPlanModule.create({
  //     data: createSubscriptionModuleInput,
  //   });
  // }
}
